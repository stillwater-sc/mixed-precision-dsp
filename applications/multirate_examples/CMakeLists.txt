# Multirate DSP demos — polyphase interpolators / decimators / rational
# sample-rate conversion. Issue #136 (audio_resampler) is the first
# entry; future multirate demos (arbitrary resampler, farrow interpolator,
# CIC decimator, etc.) get their own subdirectory here.

add_subdirectory(audio_resampler)
add_subdirectory(fractional_delay)
add_subdirectory(channelizer)
add_subdirectory(software_radio)
